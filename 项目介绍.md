# wlwl-ass 项目介绍（小白可用版）

> 这是一份从零讲起的项目介绍。不需要懂编程，跟着读完就能用起来。
>
> 如果只想 30 秒装好跑起来，跳到[第 3 节：手把手安装](#3-手把手安装)。

---

## 1. wlwl-ass 是什么

一句话：**给电脑装一个"听人话办事"的 AI 助理**。

它不是聊天机器人。和 ChatGPT、Claude 这种纯对话工具不一样的地方是——它能**真的动手**：

| 你说一句话 | 它会真的去做 |
|---|---|
| "把桌面那个 Excel 里的销售额画个柱状图" | 自己读文件、自己写代码、自己装 matplotlib、自己画图存桌面 |
| "打开淘宝搜 iPhone 16 按价格排序" | 自己开浏览器、自己点搜索框、自己点排序按钮 |
| "查一下今天微信群里有谁找我" | 自己进微信、自己翻聊天、自己总结给你 |
| "监控某只股票，跌破 20 块提醒我" | 自己装行情库、自己写监控脚本、自己挂定时任务 |

它**不预设技能**——第一次让它做新事情时它会自己摸索（装依赖、写脚本、调试），做成功之后**把这个解法记下来**，下次再让它做类似的事就直接调用，不用重新摸索。

用得越久，它会的事就越多，最后你会拥有一棵**别人都没有、只属于你这台电脑**的技能树。

### 核心数字

- 核心代码 **~3K 行**（Agent Loop 主循环只有 ~100 行）
- 只有 **9 个原子工具**（执行代码、读写文件、浏览器、问用户……）
- 上下文窗口 **<30K**（市面其他 Agent 通常 200K~1M，省 token 一个数量级）

### 它是怎么来的

wlwl-ass 是**上游 GenericAgent** 项目的更名衍生版（MIT 协议继承），原始项目地址 `github.com/lsdefine/GenericAgent`。如果你看到代码里写着 GenericAgent 字样，那是历史出处。详见 `ATTRIBUTION.md`。

---

## 2. 你需要准备什么

### 一台电脑

- Windows 10/11、macOS、Linux 都行
- Python **3.11 或 3.12 最稳**（3.10/3.13 也能跑；**别用 3.14**，部分依赖不兼容）
- 想用桌面 GUI 的话还要 Node.js ≥ 20.9 + Rust（一键脚本会自动装提示）

### 一把 API Key（必须）

wlwl-ass 自己不会"思考"——它依赖大模型 API。你需要至少**一家**模型服务商的 key：

- **国外**：Claude（Anthropic）、OpenAI（GPT）、Gemini
- **国内**：DeepSeek、月之暗面 Kimi、智谱 GLM、MiniMax
- **聚合渠道**：OpenRouter、Pollinations、自建 relay 等都支持

第一次配置可以只填一家，能跑就行；后面想加更多渠道随时再加。

### 一些可选的东西

- 浏览器（Chrome / Edge）—— 想让它自动操作网页要装一个扩展
- 手机 / 微信 / 飞书 / QQ 等 IM 账号 —— 想从手机给 Agent 发指令要配置 bot

---

## 3. 手把手安装

### Windows 用户：一键启动（推荐）

下载或克隆项目后，**双击**项目根目录里的：

```
start_from_zero.cmd        ← 或中文别名「一键启动.cmd」
```

脚本会自动：
1. 建一个虚拟环境 `.venv`
2. 装 Python 依赖
3. 装 GUI 用的 Node 依赖（首次可能要几分钟）
4. 拉起 GUI 主窗口

**第一次启动 GUI 是空白的**——进 **API 配置** 标签页加一条 key 就能开始对话。

### macOS / Linux 用户：手动三步

打开终端：

```bash
# 1. 克隆代码
git clone https://github.com/lsdefine/GenericAgent.git
cd GenericAgent

# 2. 装 Python 依赖（先建个 venv 更干净）
python3 -m venv .venv
source .venv/bin/activate
pip install -e ".[ui]"

# 3. 启动 GUI
python launch.pyw
```

> 如果暂时不想用 GUI（比如服务器没桌面环境），跳过 `.[ui]` 直接 `pip install -e .`，然后用 CLI 模式（看第 5 节）。

### 配置第一把 Key（GUI 方式，最简单）

GUI 起来后：

1. 切到 **API 配置** 标签页
2. 点 **+ 新建** 或从预设里挑一个服务商（Claude / OpenAI / DeepSeek / Kimi …）
3. 填名字（自己取，比如 `我的-deepseek`）+ API Key
4. 保存
5. 切到 **会话** 标签页 → **新建** → 输入第一句话，比如：

```
列出当前目录下所有 Python 文件
```

看到 Agent 回你内容就跑通了。

---

## 4. GUI 模式（桌面应用）

### 它是什么

一个**桌面应用**，基于 Tauri（Rust 内核）+ React 前端构建。窗口长得像 ChatGPT 客户端，但功能更多。**自 2026-05 起是唯一被支持的桌面入口**——以前的 Qt 窗口、Streamlit 网页、桌面宠物等多个旧入口已合并到这一个 GUI 里。

### 启动方式

```bash
python launch.pyw           # 任何平台都能用
start_from_zero.cmd         # Windows 一键（包含装依赖）
一键启动.cmd                # 同上，中文别名
```

### 4 个标签页

| 标签 | 干什么 |
|------|--------|
| **会话** | 多会话管理：新建/启动/停止/打开聊天窗、置顶、删除。每个会话独立项目根目录与上下文。 |
| **Bots** | 6 个 IM 平台（Telegram / QQ / 飞书 / 企业微信 / 钉钉 / 微信）的状态、启停。凭据填好 + SDK 装好 = 自动在线，不需要勾开关。 |
| **API 配置** | 多渠道 API key 管理（CRUD）。支持预设 50+ 服务商一键导入。包含 mixin（多渠道故障转移）、reasoning_effort、思考预算等高级字段。 |
| **设置** | 全局默认：默认 LLM、权限模式（auto / ask / read-only）、项目根、L4 调度等。 |

### GUI 的优势

- ✅ **零命令行**——对完全不会编程的人最友好
- ✅ **多会话并行**——左边列表切换不同任务
- ✅ **API 凭据可视化管理**——50+ 服务商预设，复制粘贴 key 就能加
- ✅ **Bot 状态一目了然**——绿灯/红灯清楚显示哪个 bot 在线
- ✅ **自动后台拉起**——填好凭据后下次启动自动连上飞书/QQ/微信，不用手动跑脚本
- ✅ **会话恢复**——意外退出的对话能从 `/continue` 找回

### GUI 的限制

- ❌ 占用资源稍多（要跑 Tauri webview + React 前端）
- ❌ 服务器（无桌面环境）跑不了
- ❌ 远程 SSH 不方便用
- ❌ 启动比纯 CLI 慢几秒

---

## 5. CLI 模式（终端命令行）

### 它是什么

一个**终端命令行工具**，叫 `wlwl`，行为对标 `claude` 和 `codex`——在任何终端里敲 `wlwl` 进入交互式对话，敲 `wlwl "做某件事"` 一次性跑完任务。

### 启动方式

装完 `pip install -e .` 之后：

```bash
wlwl                                   # 进交互 REPL（最常用）
wlwl "列出当前目录的 python 文件"        # 一次性跑完，回到终端
wlwl -p "把 README 翻成英文"             # 同上，-p 是显式写法
wlwl --llm 1                           # 用配置列表里第 2 个 LLM
wlwl --permission ask                  # 每次工具调用都问你（最安全）
wlwl --cwd /path/to/project            # 把别的目录当项目根
wlwl --version                         # 看版本

wlwl config list                       # 看已配置的 API 渠道
wlwl config presets                    # 浏览 50+ 服务商预设
wlwl config add --preset deepseek --apikey sk-...  # 从预设加一个
wlwl config use 我的-deepseek           # 切到这个渠道当默认
wlwl config probe 我的-deepseek         # 测延迟
```

如果项目目录不在 PATH 里，直接用：

```bash
python -m launcher.cli_repl            # 同 wlwl
python -m launcher.cli_repl "做某件事"   # 同 wlwl "做某件事"
```

Windows 用户：项目根有 `wlwl.cmd`，macOS / Linux 用户：项目根有 `wlwl` shell 脚本。把项目目录加进 PATH 就能任意位置敲 `wlwl`。

### CLI 内部指令（在 REPL 里输入）

| 命令 | 作用 |
|------|------|
| `/help` | 列出所有命令 |
| `/new` | 开启新对话，清空上下文 |
| `/continue` | 列出能恢复的历史会话 |
| `/continue N` | 恢复第 N 个历史会话 |
| `/quit` 或 `Ctrl+D` | 退出 |
| `Ctrl+C` | 中断当前正在跑的任务（保留 REPL） |

### CLI 的优势

- ✅ **轻量**——启动快、占资源少
- ✅ **可脚本化**——管道、重定向、cron 调用都自然
- ✅ **服务器友好**——SSH / Docker / WSL 里随便用
- ✅ **一次性任务方便**——`wlwl "做某件事"` 一行解决
- ✅ **流式输出**——边想边打字一样看 Agent 输出

### CLI 的限制

- ❌ **没有 GUI 的会话管理**——只能开一个会话
- ❌ **没法可视化看 bot 状态**（要看 `Bots` tab）
- ❌ 终端不能渲染图片，多模态结果只能存文件
- ❌ 一些复杂的多渠道编排还是 GUI 更顺手

---

## 6. GUI vs CLI 怎么选

| 我是 / 我要做 | 推荐 |
|---|---|
| 完全不会编程，想要图形界面 | **GUI** |
| 在自己电脑上日常用，要管理多个项目 | **GUI** |
| 想从手机给电脑发指令（飞书/微信 bot） | **GUI**（凭据自动起 bot） |
| 写脚本批量跑 Agent 任务 | **CLI**（`wlwl -p "..."` 写进脚本） |
| SSH 连服务器跑 | **CLI** |
| 偶尔一次性跑个任务 | **CLI**（`wlwl "..."` 一行完事） |
| 接定时任务，凌晨自动跑 | **CLI** + cron / 任务计划 |
| 想看可视化的会话历史、token 消耗 | **GUI** |
| 想体验"对话感"最强的形式 | **CLI**（更像 Claude Code / Codex） |

**重要**：CLI 和 GUI **共享同一份配置**（都读 `temp/launcher_api_configs.json`），所以你在 GUI 里配的 key，在 CLI 里 `wlwl` 也能直接用，反之亦然。

---

## 7. IM Bot 接入（可选，但很好用）

接入 IM bot 后，你**手机上随时随地**给 Agent 发指令——上班路上发"帮我查下昨天的报销有没有走完"，回家路上发"看看明天的天气，要不要带伞"。

### 支持的平台

| 平台 | 适合 | 安装 SDK |
|------|------|---------|
| **Telegram** | 海外、对接简单 | `pip install python-telegram-bot` |
| **飞书 / Lark** | 公司用，多模态支持最好（图片/文件/语音） | `pip install lark-oapi` |
| **QQ** | 国内年轻人 | `pip install qq-botpy` |
| **企业微信** | 公司内部 | `pip install wecom-aibot-sdk` |
| **钉钉** | 国企/事业单位 | `pip install dingtalk-stream` |
| **个人微信** | 最日常，扫码登录 | `pip install pycryptodome qrcode` |

### 接入步骤（以飞书为例）

1. 去[飞书开放平台](https://open.feishu.cn/)创建一个自建应用，拿到 `App ID` + `App Secret`
2. 在 GUI 的 **Bots** tab 里：
   - 选 **飞书**
   - 粘贴 `App ID` + `App Secret`
   - 填允许使用的用户 ID（`["ou_xxx"]` 或 `["*"]` 表示所有人）
3. 装 SDK：`pip install lark-oapi`
4. 重启 GUI——飞书 bot **自动在线**
5. 在飞书 App 里跟 bot 私聊 / @ 它发消息即可

> 详细配置看 `assets/SETUP_FEISHU.md`。其他平台同理——GUI Bots tab 里有引导。

### 通用聊天指令

所有 bot 都支持：

- `/new` — 开新对话清空上下文
- `/continue` — 列出能恢复的会话
- `/continue N` — 恢复第 N 个

---

## 8. 第一次使用，能玩什么

按顺序试这几条，循序渐进体验它的能力：

### 入门

```
列出当前目录的 python 文件
```

```
在桌面建一个 hello.txt，内容是 Hello World
```

```
把 README.md 翻译成英文，存为 README_en.md
```

### 让它装能力

```
请查看你的代码，安装所有用得上的 python 依赖
```

```
帮我解锁当前用户的 PowerShell ps1 执行权限      （Windows）
```

```
执行 web setup sop，解锁 web 工具
```

最后这条解锁后，它能直接操作真实浏览器（保留你登录态）：

```
打开淘宝，搜索 iPhone 16，按价格排序
```

```
去 B 站，查看我最近看过的历史视频
```

### 进阶

```
配置 ADB 环境，准备连接安卓设备            （手机自动化）
```

```
用 rapidocr 配置你的 ocr 能力并存入记忆      （OCR 读屏文字）
```

```
仿造你的 llmcore，写个调用 vision 的能力并存入记忆   （多模态看屏）
```

### 让它记住

如果你觉得某些重要事情应该长期记住：

```
把这个记到你的记忆里：我的项目主目录是 D:\Work
```

下次它就会自动在合适场景调用。

### 自我更新

```
git 更新你的代码，然后看看 commit 有什么新功能
```

它会自己 `git pull` 然后解读 commit log 告诉你新加了什么。

---

## 9. 工作原理（想知道再看）

如果你不关心原理只想用，跳过这节就行。

### 三个核心

**1️⃣ 分层记忆**

| 层 | 名字 | 干什么 |
|---|------|--------|
| **L0** | 元规则 Meta Rules | Agent 的基础行为约束 |
| **L1** | 记忆索引 Insight Index | 极简索引，用于快速路由 |
| **L2** | 全局事实 Global Facts | 长期积累的稳定知识 |
| **L3** | 任务 Skills / SOPs | 可复用的任务流程 |
| **L4** | 会话归档 Session Archive | 已完成任务提炼出的长程归档 |

**2️⃣ 9 个原子工具**

| 工具 | 干什么 |
|------|--------|
| `code_run` | 执行任意代码（**最强力的工具**——任何能力都能通过它装出来） |
| `file_read` | 读文件 |
| `file_write` | 写文件 |
| `file_patch` | 改文件 |
| `web_scan` | 看网页内容 |
| `web_execute_js` | 控制浏览器 |
| `ask_user` | 问你（人机协作） |
| `update_working_checkpoint` | 保存工作中检查点 |
| `start_long_term_update` | 触发长期记忆更新 |

**3️⃣ 主循环（agent_loop.py，~100 行）**

```
感知环境 → 推理任务 → 调用工具 → 写经验进记忆 → 循环
```

就这么简单，没了。

### 为什么省 token

很多 Agent 把所有可能用到的工具说明、所有上下文塞进 prompt，结果 200K 起步。wlwl-ass 用**分层记忆 + 极简工具集**让每次只把当下相关的东西放进上下文，平均 **<30K** 就能办事。少噪声 = 少幻觉 = 更便宜 + 更准。

---

## 10. 常见踩坑

| 现象 | 原因 / 解决 |
|------|-------------|
| `start_from_zero.cmd` 报 "Tauri toolchain not ready" | 没装 Node.js 或 Rust。装 Node.js ≥20.9（`winget install OpenJS.NodeJS.LTS`）+ Rust（`rustup.rs`） |
| 启动时报 `No usable LLM config` | 没配 API key。进 GUI **API 配置** tab 加一条，或编辑 `.env` |
| 401 Unauthorized | API key 粘贴时多了空格 / 换行。重新复制 |
| 405 / 404 探测失败但保存继续 | 部分中转 relay 不支持 `GET /models` 探测——这是正常的，对话依然能走 |
| 飞书 / TG / QQ 没自动起 | GUI **Bots** tab 看状态：「未配置」缺凭据；「SDK 缺失」装对应 pip 包 |
| GUI 已开第二次点没反应 | 单例锁端口 `19736`——切到已开的那个窗口就行 |
| 想跑后台 scheduler 定时任务 | GUI **设置** tab 里 `scheduler` 默认开；关了就不起 |
| 一启动就崩 | 多半是 Python 版本不对。**别用 3.14**，推荐 3.11 / 3.12 |
| 装依赖卡国内 pip 源 | 配清华源：`pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple` |

---

## 11. 想深入还能看什么

| 我想 | 看哪 |
|------|------|
| 30 秒装好 | `QUICKSTART.md` |
| 详细图文上手教程 | `GETTING_STARTED.md` 或[飞书图文版](https://my.feishu.cn/wiki/CGrDw0T76iNFuskmwxdcWrpinPb) |
| 看完整介绍 + 对比同类工具 | `README.md` |
| 配置文件优先级 / 高级凭据 | `docs/CONFIG.md` |
| 上游论文（原理深挖） | [arXiv:2604.17091](https://arxiv.org/abs/2604.17091) |
| Datawhale 教程版 | [Hello GenericAgent](https://datawhalechina.github.io/hello-generic-agent/) |
| 语音助理（端到端 voice loop） | `docs/RUNBOOK_voice.md` |
| 架构 / 设计决策 | `docs/architecture/` + `docs/adr/` |
| 出处与第三方借用记录 | `ATTRIBUTION.md` |
| 一起聊聊 | LinuxDo 社区 + 微信交流群 8/9/11（README 末尾有二维码） |

---

## 12. 一句话总结

**wlwl-ass** = 一个能真正动手干事、用得越久越懂你、本身只有 3K 行代码的 AI 助理。GUI 适合日常用，CLI 适合脚本和服务器，IM bot 适合远程指挥。配一把 API key 就能开始——剩下的它自己学。

祝你玩得开心。
