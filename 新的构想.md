# 助理智能体项目 

## 0. 指令目标

请按照本文档的架构设计和接口约定，实现一个**可扩展的助理智能体系统**。核心能力是：**将任意 GitHub 上的开源项目封装为可插拔工具，并支持通过声明式工作流（SOP）编排这些工具**。

------

## 1. 系统总览与设计原则

- **六边形架构**：核心领域只依赖抽象接口，所有外部库通过适配器实现。
- **插件化工具**：每个工具是一个独立模块，实现统一接口，可被自动发现和注册。
- **零代码扩展**：新增工具无需修改核心代码，只需添加适配器模块并声明依赖。
- **工作流引擎**：可解析 YAML/JSON 定义的步骤图，按序调用工具，支持串行、并行、条件分支。
- **CLI 优先**：默认以命令行交互，但核心服务通过 API 暴露，支持将来对接 GUI。

------

## 2. 必须实现的核心模块

### 2.1 工具抽象（`Tool` 接口）

定义所有工具必须实现的契约：

text

```
interface Tool {
    name: string;                           // 唯一标识
    description: string;                    // 供 LLM 理解的用途说明
    inputSchema: JsonSchema;                // 输入参数的 JSON Schema
    execute(params: Record<string, any>): Promise<ToolResult>;
}

interface ToolResult {
    success: boolean;
    data?: any;
    error?: string;
}
```



### 2.2 工具注册中心（`ToolRegistry`）

- 职责：发现、存储、查询系统中所有可用工具。
- 功能：
  - 应用启动时，自动扫描所有实现了 `Tool` 接口的类/模块，并注册。
  - 提供 `getTool(name: string): Tool` 和 `listTools(): Tool[]` 方法。
- **自动发现机制（Java/Spring示例）**：利用 `@Component` + `@ConditionalOnClass` 自动注册。如果是 TypeScript/Python，则使用装饰器注册或约定目录扫描。

### 2.3 智能体核心（`AgentCore`）

- 职责：接收用户意图，决策工具调用，管理对话上下文。
- 要求：
  - 维护 ReAct 循环（思考 → 调用工具 → 观察结果 → 继续或结束）。
  - 通过 `ToolRegistry` 获取可用工具列表和 schema，生成给 LLM 的 Function Calling 定义。
  - 处理 LLM 的响应，执行工具调用，并将结果反馈给 LLM。
- **上下文管理**：当对话历史接近模型输入限制时，自动压缩摘要（参考 Claude Code 的压缩机制）。
- **规划能力**：初始可依赖 LLM 隐式规划，后续可扩展显式 SOP 匹配。

### 2.4 工作流引擎（`WorkflowEngine`）

- 职责：解析外部 SOP 配置文件，按依赖顺序执行工具。

- 支持语法：

  - **步骤**：定义每个步骤使用的 `tool` 和 `input`。
  - **依赖**：`depends_on` 指定前置步骤。
  - **条件**：`when` 基于上下文变量判断是否执行。
  - **数据传递**：使用 `$stepId.field` 引用先前步骤的输出。

- 示例 SOP（YAML）：

  yaml

  ```
  name: "用户问题自动分类与响应"
  steps:
    - id: classify
      tool: text_classifier
      input:
        text: "$input.query"
    - id: urgent_handler
      tool: urgent_responder
      when: "$classify.priority == 'high'"
      input:
        message: "$classify.summary"
    - id: normal_handler
      tool: knowledge_search
      when: "$classify.priority == 'low'"
      input:
        question: "$input.query"
  ```

  

- **执行模式**：可串行/并行（基于依赖拓扑排序），返回最终结果和中间步骤日志。

### 2.5 CLI 交互层（`CLIInterface`）

- 默认实现：读取用户输入，调用 `AgentCore` 处理，打印结果。
- 支持参数：
  - `--tool-list`：列出所有已注册工具。
  - `--run-sop <path>`：直接运行某个 SOP 文件。
  - `--auto`：从 stdin 读取任务，自动执行循环。

------

## 3. 集成 GitHub 项目的强制规则

任何 GitHub 项目集成**必须**遵循以下步骤，以保证架构整洁：

1. **为项目创建独立适配器模块/包**（例如 `adapter-pdf-tool`）。
2. 在适配器模块中引入该 GitHub 项目的依赖（Maven/Gradle/npm 等）。
3. **实现 `Tool` 接口**，封装原始库的全部调用。
4. **所有异常必须在此层转换**为核心可理解的标准错误类型，不允许原始库异常向上传播。
5. **提供工具描述和参数 Schema**，使其能被 LLM 理解。
6. 利用框架的自动发现机制让适配器**零代码侵入**到主系统。

**禁止事项：**

- 直接修改开源项目源码。（如发现 bug，fork 并发布新包，适配器引用新包）
- 将原始库类型暴露到核心层。
- 在核心业务代码中硬编码任何具体工具的信息。

------

## 4. 技术选型建议

- **后端语言**：可选用 TypeScript/Python/Java，需支持依赖注入或模块自动发现。
- **包管理**：Monorepo 多模块结构（如 Gradle 多模块、pnpm workspace）。
- **LLM 集成**：通过统一接口对接多种 LLM 服务，至少支持 OpenAI Function Calling 格式。
- **上下文压缩**：实现一个摘要生成服务，当 token 数超过阈值时自动压缩历史。
- **安全**：工具执行应有沙箱机制，尤其是集成 CLI 代理工具时（如限制工作目录、禁止危险命令）。

------

## 5. 可选扩展（高阶需求）

### 5.1 集成 Claude Code CLI 或 Codex CLI 作为“万能工具”

- 创建一个特殊工具 `UniversalAgentTool`，内部通过子进程调用 Claude Code CLI，传递自然语言任务。
- 该工具必须在隔离的进程中运行，并设置超时和权限限制。
- **更推荐的方式**：如果可能，直接通过 MCP 协议将该 CLI 的工具列表引入，而不是整个 CLI 作为黑盒代理。

### 5.2 可视化工作流编排（GUI）

- 未来可在上述核心 API 基础上搭建 Web 前端。
- 前端仅生成 SOP 的 YAML 配置文件，交由后端引擎执行，不影响核心稳定性。

------

## 6. 验收标准

1. 能在不修改核心代码的前提下，通过新增模块集成一个 GitHub 项目（例如一个 PDF 操作库），并且该工具自动出现在工具列表中，可被 LLM 调用。
2. 编写一条包含 2 个以上工具调用的 SOP 配置，工作流引擎能正确解析并顺序执行，前一步的输出被后一步引用。
3. 智能体核心可以基于工具列表和用户意图，自动选择并调用合适的工具，完成单步或多步任务。
4. CLI 可启动智能体，输入自然语言指令后得到预期结果。

------

**请根据以上规范，生成完整的项目骨架、核心接口定义和关键实现代码，并提供一个示例适配器（如 PDF 合并工具）的完整实现。**

另外：可参考项目：
https://github.com/tinyhumansai/openhuman.git

https://github.com/K-Dense-AI/scientific-agent-skills.git

https://github.com/supertone-inc/supertonic.git

https://github.com/joeseesun/qiaomu-anything-to-notebooklm.git

https://github.com/anthropics/financial-services.git

https://github.com/CloakHQ/CloakBrowser.git

https://github.com/Hmbown/DeepSeek-TUI.git

https://github.com/apernet/hysteria.git

保留飞书等功能，让我可以用手机来安排任务，交互等操作。